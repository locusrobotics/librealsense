name: build_librealsense_deb_packages

on:
  workflow_dispatch:
    inputs:
      source_ref:
        description: Git ref to build, such as v2.53.1
        required: true
        default: v2.53.1
        type: string

permissions: read-all

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow registered. Real implementation pending."
